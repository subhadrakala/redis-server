# redis-server
Own Redis Server. https://codingchallenges.fyi/challenges/challenge-redis


## Run locally
Install dependencies: `npm install`
Type check: `npm run typecheck`
Build TypeScript to JavaScript: `npm run build`
Test runner: run `node dist/index.js` or `npm run dev` briefly to confirm server starts and listens.